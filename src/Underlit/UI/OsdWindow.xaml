<Window x:Class="Underlit.UI.OsdWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        IsHitTestVisible="False"
        ResizeMode="NoResize"
        Width="280"
        Height="48"
        Opacity="0"
        WindowStartupLocation="Manual"
        Title="Underlit">
    <!--
        Layout strategy:
          • Window IS the visible flyout (280x48). DWM applies its backdrop, rounded
            corners, and shadow to the whole window. No extra margin = no "huge frame".
          • Background="Transparent" lets the DWM backdrop bleed through wherever no
            opaque pixel is drawn.
          • The "TintLayer" Border provides a per-mode tint over the backdrop.
          • Liquid Glass adds two more visual layers on top of the tint: a top
            specular gradient and a bottom edge sheen, both purely white at varying
            alpha so they don't fight dark/light mode.
    -->

    <Grid x:Name="WindowRoot">

        <!-- Tint layer. Per-mode background colour. -->
        <Border x:Name="TintLayer" CornerRadius="8"/>

        <!-- Liquid Glass: top-edge specular highlight (white grad on top ~45%). -->
        <Border x:Name="GlassSpecular"
                CornerRadius="8"
                IsHitTestVisible="False"
                Visibility="Collapsed">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Offset="0.00" Color="#66FFFFFF"/>
                    <GradientStop Offset="0.18" Color="#28FFFFFF"/>
                    <GradientStop Offset="0.50" Color="#00FFFFFF"/>
                    <GradientStop Offset="1.00" Color="#00FFFFFF"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Liquid Glass: bottom-edge sheen (faint white near bottom for "lens" depth). -->
        <Border x:Name="GlassBottomSheen"
                CornerRadius="8"
                IsHitTestVisible="False"
                Visibility="Collapsed">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Offset="0.00" Color="#00FFFFFF"/>
                    <GradientStop Offset="0.70" Color="#00FFFFFF"/>
                    <GradientStop Offset="1.00" Color="#22FFFFFF"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Subtle 1px border ring (theme-coloured in code). -->
        <Border x:Name="EdgeRing"
                CornerRadius="8"
                BorderBrush="#1FFFFFFF"
                BorderThickness="1"
                IsHitTestVisible="False"/>

        <!-- Content: icon + bar -->
        <Grid Margin="12,0,14,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="22"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Width="22" Height="22"
                  VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBlock x:Name="IconText"
                           FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                           FontSize="18"
                           Foreground="#FFFFFFFF"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Text="&#xE706;"/>
                <Path x:Name="CandleIcon"
                      Visibility="Collapsed"
                      Fill="#FFFFFFFF"
                      Stretch="Uniform"
                      Width="22" Height="22"
                      VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path.Data>
                        <GeometryGroup FillRule="Nonzero">
                            <PathGeometry>
                                <PathFigure StartPoint="7,0.5" IsClosed="True">
                                    <BezierSegment Point1="10.5,2.5" Point2="10.5,5.5" Point3="7,6.5"/>
                                    <BezierSegment Point1="3.5,5.5" Point2="3.5,2.5" Point3="7,0.5"/>
                                </PathFigure>
                            </PathGeometry>
                            <RectangleGeometry Rect="6.55,6.5,0.9,2"/>
                            <RectangleGeometry Rect="4.75,8.5,4.5,9" RadiusX="0.8" RadiusY="0.8"/>
                        </GeometryGroup>
                    </Path.Data>
                </Path>
            </Grid>

            <Grid Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center"
                  Height="4" x:Name="BarRoot" RenderTransformOrigin="0.5,0.5">
                <Grid.RenderTransform>
                    <TranslateTransform x:Name="BarSlideTransform" Y="0"/>
                </Grid.RenderTransform>

                <Grid x:Name="BrightnessBar">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="TrackLeft"  Grid.Column="0" CornerRadius="2,0,0,2" Background="#33FFFFFF"/>
                    <Border Grid.Column="0" x:Name="FillLeft"
                            HorizontalAlignment="Right" CornerRadius="2,0,0,2"
                            Background="#FFE5B675" Width="0"/>
                    <Border x:Name="TrackRight" Grid.Column="1" CornerRadius="0,2,2,0" Background="#33FFFFFF"/>
                    <Border Grid.Column="1" x:Name="FillRight"
                            HorizontalAlignment="Left" CornerRadius="0,2,2,0"
                            Background="#FFFFFFFF" Width="0"/>
                    <Border x:Name="MidMarker"
                            Grid.ColumnSpan="2"
                            Width="2" Height="14"
                            Background="#B3FFFFFF"
                            CornerRadius="1"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                </Grid>

                <Grid x:Name="WarmthBar" Visibility="Collapsed">
                    <Border x:Name="WarmthTrack" CornerRadius="2" Background="#33FFFFFF"/>
                    <Border x:Name="WarmthFill"
                            HorizontalAlignment="Left"
                            CornerRadius="2"
                            Width="0">
                        <Border.Background>
                            <LinearGradientBrush x:Name="WarmthGradient"
                                                 StartPoint="0,0" EndPoint="1,0">
                                <GradientStop x:Name="WarmthStartStop" Offset="0" Color="#FFFF5A00"/>
                                <GradientStop x:Name="WarmthEndStop"   Offset="1" Color="#FFFFF9FB"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
